name: Deploy Next.js portfolio to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: bober4uk-portfolio-updated/bober4uk-portfolio
        run: npm ci

      - name: Configure Next.js for custom domain
        working-directory: bober4uk-portfolio-updated/bober4uk-portfolio
        run: |
          cat > next.config.mjs <<'EOF'
          /** @type {import('next').NextConfig} */
          const nextConfig = {
            reactStrictMode: true,
            output: "export",
            trailingSlash: true,
            images: { unoptimized: true }
          };
          export default nextConfig;
          EOF

      - name: Build
        working-directory: bober4uk-portfolio-updated/bober4uk-portfolio
        run: npm run build

      - name: Preserve custom domain
        run: cp CNAME bober4uk-portfolio-updated/bober4uk-portfolio/out/CNAME

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: bober4uk-portfolio-updated/bober4uk-portfolio/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
